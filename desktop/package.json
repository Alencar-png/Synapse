{
  "name": "meeting-processor-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "App desktop do Meeting Processor: solte um vídeo, receba a transcrição.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test \"tests/*.test.js\"",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "electron": "^43.4.0"
  }
}
